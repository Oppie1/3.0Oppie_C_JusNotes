#include<stdio.h>
#include<stdlib.h>



//Order of Operations: C follows standard math precedence rules (like PEDMAS). Use parentheses
//to explicitly control the order in which an expression is evaluated.


int main() 
	{

		//Declare an integer variable called "a" and set it equal to an expression where multiplication happens first even 
		//though it takes place after to values being added together.
		//CODE:

		//call a print function to show the value of your integer variable to the screen using a sentence and 
		// format access specifier as well as the variable in second parameter.
		//CODE:

		//'a' can be reassigned at any point without repeating 'int' data type before it. Can just say 'a' = this.
		//The data type is only needed when first declared.
		//So now using the same variable a and expression put parenthesis around the addition portion while leaving
		//the multiplication portion as is. Wrapping (number 1 + number 2) in parenthesis forces that addition
		//evaluated first then the multiplication step is done.
		//CODE:

		// call a print function to show the value of your integer variable to the screen using a sentence and
		//a format access specifier as well as the variable.  Notice the difference in both outputs.
		//CODE:

	}
