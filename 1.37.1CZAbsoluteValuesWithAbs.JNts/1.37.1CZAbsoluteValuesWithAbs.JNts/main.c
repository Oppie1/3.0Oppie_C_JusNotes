#include<stdio.h>
#include<math.h>



int main()
{
	//The sqrt() function calculates the square root of a number, whether it's a integer or a decimal.
	//Example sqrt(164) = 12.896248. By default, "%f" displays 6 decimal places.
	//Call a printf() function that shows a decimal variable to screen (float format specifier) in the 
	//first slot/argument with new line after, and in the second argument use the square root sqrt() function
	//with a number as its argument e.g. 164.
	//CODE:

	//The pow() function computes exponentiation: it raises the first argument to the power of the second.
	//Example: pow(13,2) calculates 13 squared(13^2 = 169).
	//Here call a printf() with the first argument being the format specifier for a float and in the 
	//second argument a call to the power ->pow() function with two comma separated values representing
	//the base x and raised to the power/exponent. e.g. 13 (base) raised to 2 (power/exponent)
	//13^2= 169
	//CODE:

}