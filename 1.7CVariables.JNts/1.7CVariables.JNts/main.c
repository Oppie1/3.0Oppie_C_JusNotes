#include<stdio.h>




/*Variables are named storage locations in memory that hold values. Think of them like labeled
boxes where you can store and retrieve data, similar to how you use variables in algebra*/


int main()
{

	//Declare 3 integer variables: age, currentYear, birthYear.
	//CODE:

	//Now initialize the integer variables with specific values (years). These values are set directly in the code 
	//not entered by the user.
	//CODE:

	//Calculate age by subtracting birth year from the current year. Here we assign the variable age to
	//as the answer to this expression. Remember, when assigning variables to a value/expression or whatever
	//the assigned goes on left hand side. So here age would go on the left hand side and expression on right.
	//In C, we use the assignment operator (=) to store the result in age. The right side of the equation is 
	//evaluated first, then stored in the variable on the left. General syntax: variable = value1 (operator) value2
	//Operators include + (addition), (-) subtraction, * (multiplication, / (division)
	//Write an expression that sets the age equal to the result of the current year minus the birth year.
	//CODE:
	

	//When this line executes, the calculation proceeds as follows and is stored in int age:
	//age = 2026 - 1983, which results in age = 43.

	//Print message to screen that say Kanopi is... years old and use the age variable and data type specifier to do so.
	//CODE:

}