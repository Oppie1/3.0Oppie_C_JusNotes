#include<stdio.h>
#include<stdlib.h>



int main() {

	//Declare a char array called 'name' with 14 elements and assign it the string "Kanopi Lotus"
	//The array size must be large enough to hold all characters PLUS one extra space for the 
	//null terminator '\0', which signals the end of the string to the complier.
	//CODE:
	

	//Print a greeting message to the screen using the string stored in 'name'. The format specifier
	//%s tells printf() to insert the string value 'name' in place of %s within the output.


	//Declare a char array called 'food' with 10 elements and initialize it to "soybean". Always size
	//your array to fit the longest string you expect to store, including the null terminator. Exceeding
	//the array size causes a buffer overflow, which lead to unpredictable behavior or crashes.
	//CODE:

	
	//Now show your information to the screen using a sentence "_ is the best food in the world. 
	//CODE:
	

	//strcpy() (string copy) from <string.h> overwrites the current contents of a char array.
	//Remember all strcpy() is a function with parameters. Here just two.
	//with a new string value. Here, replace "soybean" with "peanuts". Be sure the destination array is large
	//enough to hold the new string.
	//CODE:


	//Print the updated value of 'food' to confirm strcpy() successfully replaced "soybean" with
	//"peanuts" in the array.
	//CODE:

	
}