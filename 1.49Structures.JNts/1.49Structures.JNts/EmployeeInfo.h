//A struct groups related variables (called members) under a single name
//You can defines multiple structs with different names to represent different entities.

//Declare a struct named user.
//CODE:

	//These are the members of the struct - each one holds a specific piece of data.
	//Declare an uninitialized int variable named userID.
	//CODE:

	//A char array of 25 elements to store the user's first name 
	//(up to 24 characters + null terminator).
	//Declare an uninitialized char array that has 25 elements named firstName.
	//CODE:

	//A char array of 25 elements to store the user's last name
	//(up to 24 characters + null terminator.
	//Declare a char array uninitialized  named lastName with 25 elements
	//CODE:

	//Declare an uninitialized int variable to hold age;

	//Declare an uninitialized float variable to hold weight.

//Struct are especially useful representing real-world entities like people or employees.
//Since you can bundle all related attributes together - similar to a row in a table.