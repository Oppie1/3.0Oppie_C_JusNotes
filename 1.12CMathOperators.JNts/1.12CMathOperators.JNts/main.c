#include<stdio.h>
#include<stdlib.h>



int main()

{
	//The 'int' data type stores numbers only - no decimals allowed. This means any division that produces
	//a fractional result will be truncated (the decimal is simply dropped, not rounded.
	//Declare 4 integer variables a b and c two being positive, one negative.The create one to hold weight.
	//CODE:

	//86/21 = 4.095..., but since both values are ints, only 4 is kept.
	//call (invoke) a print function out to an integer out to the screen that takes a int and a expression where
	//it divides the two positive variable as its second parameter. ("A format string", an expression.)
	//CODE:
	
	//Negative values behave the same way - decimal is dropped but the sign of the result is preserved.
	//-81/27 = -3.856... becomes -3.
	//call a print function that takes int format specifier as the first parameter and an expression where it 
	//divided a positive variable by the negative variable as the second parameter. A format string, an expression.
	//CODE:

	//The 'float' data type supports decimal (floating-point) values, making it suitable for division that requires
	//fractional precision. 
	//Declare two float variable initialized to decimal .0 numbers
	//CODE:

	//Using %f without any precision specifier prints 6 decimal places by default.
	//Call a print function that shows the first float variable format specifier as the first parameter and an 
	//expression where it divides the two floats together. float/float. A format string, expression.
	//CODE:

	//Adding a precision specifier like %.2f limits the output to 2 decimal places. You can use .2, .4, ect. to 
	//control exactly how many decimals are shown.
	//CODE:

	//The modulo operator (%) returns the remainder of division, not the quotient. For example, 10/4 = 2 with a remainder
	//of 2, so the expression evaluates to 2.
	//Call a print function that uses a sentence about the 4 watermelons weighing 10 pounds. Which is 2 more pounds than
	//you thought.
	//CODE:
	
}