#include<stdio.h>



int main() {

	//The printf() function displays text to screen. To insert a value into your output, use a format
	//specifier like %d for integer that will match the parameter so it will print that value to the screen.
	//i.e.After the string, use a comma followed by the value you want to insert. Use \n to create go to a new line
	//in your output.
	//Display a message to the screen about how many slices of pie you ate last night.
	//CODE:

	

	//For string values (text), use %s as your format specifier instead of %d. Always wrap strings in double quotes
	//("like this"). You can use multiple format specifier in a single printf() call and provide matching values
	//in the SAME order.
	//Print a message to the screen that uses 2 string data specifiers and a sentence to say how good Kanopi is at 
	//being a C++ programer.
	//CODE:

	//To control decimal places in floating-point numbers, use the format specifier %.#f, where # is the number of
	//decimal places you want to display. For example, %2.f shows 2 decimal places, and %.4f shows 4 decimal places.
	//Without specifying a number, %f defaults to 6 decimal places.
	//Demonstrate using %f and %.f to show how to show no dicmal places of pi (to 8 digits), 2 or 3 decimal places.
	//CODE:
 
	//CODE:

	//CODE:


}