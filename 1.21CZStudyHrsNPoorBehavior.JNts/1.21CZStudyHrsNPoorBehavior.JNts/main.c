#include <stdio.h>
#include <stdlib.h>



int main()
{
	//Declare two uninitialized variable to hold values for hours a student studied and
	//another for if they got any marks for bad behavior.
	//CODE:

	//call a printf() function to ask user how many hours they studied. 
	//CODE:

	//call a scanf_s() function to scan in the information from the user input. &
	//CODE:

	//Now use the same logic to prompt user and see in information into the
	//poor behavior variable.
	//CODE:

	//Now use an if statement to check if the hours studied more than 10 hours and
	//another to see if they got ANY && marks for bad behavior. If they studied for 
	//more than 10 hours AND they got exactly 0 == marks for bad behavior then print
	//out that they were a good student to the screen. If they did not pass either
	//of if conditions use an else statement to tell them to go see the counselor.
	//REMEMBER you will need to use parenthesis for both arguments and then parenthesis
	//to contain both arguments together.
	//CODE:
	
	//CODE:

	//CODE:
	
}