#include<stdio.h>
#include<stdlib.h>	


int main() {

//Declare an integer variable called "age" to hold the user's input.
//It is left uninitialized because its value will be provided by the user at runtime.
//CODE:

//Call a print function to a prompt the user for their age. 
//The "\n" moves the cursor to a new line after the message is printed.
//CODE:

//Read the integer the user types and store it in the variable "age" while using the
//"&" operator  which passes the memory address of "age" to scanf_s, telling it exactly
//where in memory to write the value. Without "&", the value could not be stored correctly.
//CODE:

//An if statement evaluates the condition in parentheses. If the condition is true, the
//code inside the curly braces {  } runs. If the condition is false, the codeblock is skipped
//entirely and execution continues on the if codeblock. Here, if "age" is greater than 18
//or greater, the user is granted access. 
//Use an if statement to validate whether the person is greater than or equal to 18
//If they are call a print function to say they may enter the website to the screen.
//CODE:

//This second if statement handles the opposite case. If the first condition was false
//(age was below 18), this condition will be true, and the message below will be printed
//to inform the user they are not old enough. Create an if statement that does this along
//wit a corresponding sentence in the if block that displays to screen.
//CODE:

}